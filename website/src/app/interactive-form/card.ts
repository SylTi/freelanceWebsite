export class Card {
    title: string;
    icon: string;
    tooltip: string;

    constructor(title: string, icon: string, tooltip: string) {
        this.title = title;
        this.icon = icon;
        this.tooltip = tooltip;
    }
}
